---
title: 使用字体压缩模块，解决中文字体包过大的方法
published: 2026-08-06T23:13:43
description: ".ttf字体文件体积庞大，将其压缩为.woff2字体文件，可以大幅减小字体文件的体积，从而加快网站加载的速度"
image: "./146681209_p0_master1200.png"
tags: ["网页"]
category: 网页
pinned: false
draft: false
author: NF3eb
---

<small>封面pid：[146681209](https://www.pixiv.net/artworks/146681209)~~ソルト好可爱啊啊啊wwwwww盐巴盐巴盐巴盐巴~~</small>

---
# 前言
TTF<br>
>TTF（TrueTypeFont）是Apple 公司和Microsoft公司共同推出的字体文件格式，随着 Windows 的流行，已经变成最常用的一种字体文件表示方式。<br>

这种格式的字体文件体积比较大，以思源宋体为例，字体文件可以达到 24MB+，通常只用作安装到计算机中的字体，或者在网页中设备不支持 WOFF2 字体情况的兜底处理。<br>

WOFF & WOFF2<br>
>Web开放字体格式（Web Open Font Format，简称WOFF）是一种网页所采用的字体格式标准。此字体格式发展于2009年，由万维网联盟的Web字体工作小组标准化，现在已经是推荐标准。此字体格式不但能够有效利用压缩来减少文件大小，并且不包含加密也不受DRM（数字著作权管理）限制。<br>

这是专门给网页使用的字体格式，体积非常小，实测压缩思源宋体字体文件，可以把体积压缩到 OTF 字体 70% 的大小。<br>

在Mizuki主题的[官网界面](https://docs.mizuki.mysqil.com/Basic-Layout/font/#%E5%AD%97%E4%BD%93%E5%AD%90%E9%9B%86%E4%BC%98%E5%8C%96)，可以看到Mizuki已经内置了字体子集优化功能，即将`.ttf`字体压缩成`.woff2`字体，大幅减小最终字体文件的大小，从而加快网页加载速率。<br>

当前文档中的设置方法仍然保留在旧版，通过配置文件`src\config.ts`和属性`enableCompress`来启用字体压缩。然而`config.ts`文件已被拆分为多个文件，在Commit `4c1c277`之后，`siteConfig.ts`也不再负责管理字体，而是使用Astro Font API，在`astro.config.mjs`中对字体进行管理。

此外，还存在以下问题：
- 仍然在读取文件`siteConfig.ts`以及不存在的属性`enableCompress`；
- 整个模块完全没有接入`build`的流程，完全不会生效；

因此此文章对此模块进行重构，使其能够实现字体压缩的功能。<br>
<small>由于本人完全不会javascript，本文章中代码均由AI生成QAQ</small><br>

---
# 分析
原先的字体压缩模块代码存储在目录`.\scripts\compress-fonts`中：
```
├─astro.config.mjs        存储字体信息
└─scripts
   └─compress-fonts
      ├─config-parser.js    配置解析（单次读取siteConfig.ts，缓存后分发）
      ├─css-rewriter.js     CSS 重写（dist/ 中ttf 引用替换为woff2）
      ├─font-compressor.js  字体压缩（Fontmin子集化+ttf->woff2 转换）
      ├─index.js            入口（串联compress->rewrite和调用）
      ├─text-collector.js   文本采集（8个来源：本地文件+3个远程API+常用字符）
      └─utils.js            共享工具函数（文件遍历、字符串提取、Markdown 解析）
```
我们主要更改的是这五个文件：
- `config-parser.js`：获取字体过程存在缺陷，需要进行重写；
- `css-rewriter.js`：原先的覆写程序存在部分问题，对其进行重写，此外实际上要覆写的文件不止`.css`文件，还包含`.html`文件，因此易名为`file-rewriter.js`；
- `font-compressor.js`：修改部分变量引用即可；
- `index.js`：设置触发时机在build完成之后；
- `astro.config.mjs`：添加对此模块的调用；

`text-collector.js`和`utils.js`无需更改，保持原样即可。<br>

大致的流程可以表现为这样：
```
文本采集
   ↓
配置解析
   ↓
压缩字体
   ↓
覆写引用
   ↓
删除.ttf文件
```
>[!note]
>Astro Font API在生成字体文件时，会在字体名称后添加一串哈希值，因此简单的覆写引用不会生效，必须先找出 **哈希值 -> 字体** 的映射，才能完成覆写引用。这样也能防止误伤其他无关字体的引用。<br>

具体到各个文件的职责如下：
1. `text-collector.js`收集所有使用到的文本，并将其传给`font-compressor.js`；
2. `config-parser.js`中函数`getFontConfigs()`收集`astro.config.mjs`中配置的本地字体，实现配置解析，并将其传给`font-compressor.js`；
3. `font-compressor.js`中函数`compressFonts()`遍历每一种字体，并将每一种字体的`.ttf`文件压缩为`.woff2`文件；
4. `css-rewriter.js`先取样扫描一个文件，找到 哈希值-> 字体 的映射；
5. 利用此映射，将所有相关文件的`hash.ttf`引用覆写为`.woff2`
6. 删除所有`hash.ttf`

---
# 实现
在这之前，需要先在 bash 界面安装 acorn 和 fontmin 的依赖：
```bash
npm install acorn
pnpm add -D fontmin
```
## 接入流程
先在`index.js`中引出最终的接口函数`fontOptimizer()`，并配置hooks使其在build完成后执行：
```js
//index.js
import { compressFonts } from "./font-compressor.js";
import { updateFileFontReferences } from "./css-rewriter.js";

export function fontOptimizer() {
	return {
		name: "font-optimizer",
		hooks: {
			"astro:build:done": async () => {
				console.log("Optimizing fonts start...");
				await compressFonts();
				await updateFileFontReferences();
				console.log("✓ Font optimization completed!");
			},
		},
	};
}
```
然后在`astro.config.mjs`中引用此模块，在`intergration`中添加我们的字体压缩模块：
```mjs
//astro.config.mjs
import { fontOptimizer } from "./scripts/compress-fonts/index.js";

export default defineConfig({
  ...
  integrations: [
    ...
		fontOptimizer(),
	],
  ...
});
```
## 文本采集
保持`text-collector.js`为[原样](https://github.com/LyraVoid/Mizuki/blob/master/scripts/compress-fonts/text-collector.js)即可。

## 配置解析
字体信息已经转移到`astro.config.mjs`，但是语言设置仍然在`siteConfig.ts`中，因此不能将所有的`siteConfig.ts`均替换成`astro.config.mjs`，而应该仅更改函数`getFontConfigs()`中的读取路径。<br>
通过正则匹配文本块解析配置不是可靠方案，因为嵌套结构会越来越复杂。因此在这个场景下，AST解析比正则更适合。下面使用**Acorn**实现，我们需要用到Acorn的`parse`函数：<br>
首先准备工具函数：
```js
//config-parser.js
import {parse} from "acorn";
//readSiteConfig()和getLang()无需更改

// 查找 fonts 数组
function findFontsArray(node) {
	if (!node || typeof node !== "object") {
		return null;
	}
	if (
		node.type === "Property" &&
		node.key.name === "fonts" &&
		node.value.type === "ArrayExpression"
	) {
		return node.value;
	}
	for (const key in node) {
		const result = findFontsArray(
			node[key]
		);
		if (result) {
			return result;
		}
	}
	return null;
}
// 获取对象属性
function getProperty(obj, name) {
	if (!obj || obj.type !== "ObjectExpression") {
		return null;
	}
	return obj.properties.find(
		p =>
			p.key.name === name
	);
}
// 获取字符串属性
function getLiteralProperty(obj, name) {
	const prop = getProperty(obj,name);
	return prop?.value?.value ?? null;
}
// 判断 provider.fontProviders.local()
function isLocalProvider(node) {
	return (
		node.type === "CallExpression" &&
		node.callee.type === "MemberExpression" &&
		node.callee.object.name === "fontProviders" &&
		node.callee.property.name === "local"
	);
}
```
随后对主要函数`getFontConfigs()`进行重写：
```js
//config-parser.js
export function getFontConfigs() {
	const configPath = path.join(ROOT_DIR, "astro.config.mjs");
	const content = fs.readFileSync(configPath, "utf8");
	const ast = parse(content, {
		sourceType: "module",
		ecmaVersion: "latest",
	});
	const fonts = [];
	// 查找 fonts 数组
	const fontsArray = findFontsArray(ast);
	if (!fontsArray) {
		return fonts;
	}
	for (const item of fontsArray.elements) {
		// 只处理对象
		if (!item || item.type !== "ObjectExpression") {
			continue;
		}
		// 判断 provider 是否 local()
		const provider = getProperty(item,"provider");
		if (!provider || !isLocalProvider(provider.value)) {
			continue;
		}
		const name = getLiteralProperty(item,"name");
		const files = [];
		const options = getProperty(item,"options");
		if (options) {
			const variants = getProperty(options.value,"variants");
			if (variants) {
				for (const variant of variants.value.elements) {
					const src = getProperty(variant,"src");
					if (!src) {
						continue;
					}
					for (const element of src.value.elements) {

						const srcPath = element.value;
						const fileName =
							path.basename(srcPath);
						const ext =
							path.extname(fileName);
						const baseName =
							path.basename(
								fileName,
								ext
							);
						files.push({
							src: srcPath,
							absolutePath:
								path.resolve(
									ROOT_DIR,
									srcPath
								),
							fileName,
							baseName,
							ext,
							woff2Name:
								`${baseName}.woff2`,
						});
					}
				}
			}
		}
		if (files.length) {
			fonts.push({
				name,
				type: "cjkFont",
				files,
			});
		}
	}
	console.dir(fonts, {
		depth: null
	});
	return fonts;
}
```
## 压缩字体
`font-compressor.js`中无需做过多更改，仅需修改部分变量名称即可（`absolutePath`，`baseName`，`woff2Name`等）：
```js
//font-compressor.js
export async function compressFonts() {
	try {
		const fonts = getFontConfigs();
		if (fonts.length === 0) {
			console.log(
				"⚠ No fonts to compress (enableCompress=false or localFonts is empty)",
			);
			return;
		}
		console.log(`Found ${fonts.length} font configs to compress`);
		const distDir = path.join(ROOT_DIR, "dist");
		if (!fs.existsSync(distDir)) {
			console.log(
				"⚠ dist directory does not exist, please run astro build first",
			);
			return;
		}
		const distFontDir = path.join(distDir, "_astro/fonts");
		if (!fs.existsSync(distFontDir)) {
			fs.mkdirSync(distFontDir, { recursive: true });
		}
		const cjkText = await collectText();
		const asciiText = getAsciiCharset();
		console.log("Starting font compression...");
		let totalOriginalSize = 0;
		let totalCompressedSize = 0;
		let processedCount = 0;
		const errors = [];
		for (const fontConfig of fonts) {
			const text =
				fontConfig.type === "asciiFont" ? asciiText : cjkText;
			for (const file of fontConfig.files) {
				const fontSrc = file.absolutePath;
				const ext = file.ext.toLowerCase();
				const baseName = file.baseName;
				if (!fs.existsSync(fontSrc)) {
					const errorMsg = `❌ Config error [${fontConfig.type}]: Font file does not exist\n   In config: "${file.fileName}"\n   Expected path: public/assets/font/${fontFile}\n\n   Please check:\n   1. Is the filename correct (case sensitive)?\n   2. Is the file in public/assets/font/?\n   3. Is ${fontConfig.type}.localFonts in src/config/siteConfig.ts correct?`;
					errors.push(errorMsg);
					console.log(`\n${errorMsg}\n`);
					continue;
				}
				const originalSize = fs.statSync(fontSrc).size;
				totalOriginalSize += originalSize;
				if (ext === ".woff2" || ext === ".woff") {
					console.log(
						`⚠ Skipping ${file.fileName} (already web-optimized format)`,
					);
					fs.copyFileSync(fontSrc, path.join(distFontDir, file.fileName));
					totalCompressedSize += originalSize;
				} else if (ext === ".ttf" || ext === ".otf") {
					console.log(`Compressing ${file.fileName}...`);
					const fontmin = new Fontmin()
						.src(fontSrc)
						.use(
							Fontmin.glyph({
								text,
								hinting: false,
							}),
						)
						.use(
							Fontmin.ttf2woff2({
								clone: false,
								deflate: true,
							}),
						)
						.dest(distFontDir);
					await new Promise((resolve, reject) => {
						fontmin.run((err, files) => {
							if (err) reject(err);
							else resolve(files);
						});
					});
					const compressedFile = path.join(
						distFontDir,
						file.woff2Name,
					);
					if (fs.existsSync(compressedFile)) {
						const compressedSize =
							fs.statSync(compressedFile).size;
						totalCompressedSize += compressedSize;
						const reduction = (
							(1 - compressedSize / originalSize) *
							100
						).toFixed(2);
						console.log(
							`✓ ${file.fileName} → ${file.woff2Name} (${(compressedSize / 1024).toFixed(2)} KB, reduced ${reduction}%)`,
						);
						processedCount++;
					}
				} else {
					console.log(
						`⚠ Unsupported font format, skipping: ${file.fileName}`,
					);
				}
			}
		}
		if (errors.length > 0) {
			console.log("\n❌ Font compression encountered errors!");
			console.log(`${errors.length} errors, please fix and retry.\n`);
			const fontDir = path.join(ROOT_DIR, "public/assets/font");
			if (fs.existsSync(fontDir)) {
				const actualFiles = fs
					.readdirSync(fontDir)
					.filter((f) =>
						[".ttf", ".otf", ".woff", ".woff2"].includes(
							path.extname(f).toLowerCase(),
						),
					);
				if (actualFiles.length > 0) {
					console.log("Available font files:");
					for (const f of actualFiles) console.log(`  - ${f}`);
				} else {
					console.log("  (font directory is empty)");
				}
			}
			process.exit(1);
		}
		if (processedCount > 0) {
			const totalReduction = (
				(1 - totalCompressedSize / totalOriginalSize) *
				100
			).toFixed(2);
			console.log("\n✓ Font compression complete!");
			console.log(
				`  Files processed: ${processedCount}, Overall reduction: ${totalReduction}%`,
			);
		} else {
			console.log("\n⚠ No font files processed");
		}
	} catch (error) {
		console.error("❌ Font compression failed:", error);
		process.exit(1);
	}
}
```
## 覆写引用
要修改的引用有以下几种，他们存在于各个`.html`文件中：
```html
<style>
@font-face
{font-family:MarukoGothicCJKsc-Medium-d7eefb91b0e1b4cf;
src:url("/_astro/fonts/54bf205c80be75a9.ttf") format("truetype");
↑↑↑要修改的在这里！
font-display:swap;
font-weight:500;
font-style:normal;}
@font-face
{font-family:"MarukoGothicCJKsc-Medium-d7eefb91b0e1b4cf fallback: Arial";
src:local("Arial");
font-display:swap;
font-weight:500;
font-style:normal;
size-adjust:99.8204%;
ascent-override:116.2087%;
descent-override:28.8518%;
line-gap-override:0%;}
:root{--font-body:MarukoGothicCJKsc-Medium-d7eefb91b0e1b4cf,"MarukoGothicCJKsc-Medium-d7eefb91b0e1b4cf fallback: Arial",sans-serif;}
</style>

<link rel="preload" href="/_astro/fonts/54bf205c80be75a9.ttf" as="font" type="font/ttf" crossorigin>
↑↑↑要修改的在这里！

<style>
@font-face
{font-family:Loli-3090eb66b07d2a39;
src:url("/_astro/fonts/0e20c4acea630827.ttf") format("truetype");
↑↑↑要修改的在这里！
font-display:swap;
font-weight:400;
font-style:normal;}
@font-face
{font-family:"Loli-3090eb66b07d2a39 fallback: Arial";
src:local("Arial");
font-display:swap;
font-weight:400;
font-style:normal;
size-adjust:111.1224%;
ascent-override:91.9291%;
descent-override:29.9046%;
line-gap-override:0%;}
:root{--font-cjk:Loli-3090eb66b07d2a39,"Loli-3090eb66b07d2a39 fallback: Arial",sans-serif;}
</style>
```
我们需要更改的就是`src:url`和`href=`中的内容，但是用简单的正则匹配容易误伤到其他字体。<br>
注意到`font-family:MarukoGothicCJKsc-Medium-d7eefb91b0e1b4cf;`后紧跟`src:url("/_astro/fonts/54bf205c80be75a9.ttf")`可知：
- Astro Font API在处理字体后会在其用户标签后添加一串哈希值，注意用户标签是指`astro.config.mjs`中的`name`元素，而不是字体文件的名称；
- 将字体文件名哈希化后会存放在目录`dist\_astro\fonts`下；

那么，我们的思路就很清晰了：
```
遍历所有字体
      ↓
采样扫描一个文件
      ↓
找到其中的font-family特征以及之后的src:url
      ↓
解析出 哈希值 -> 字体 的映射
      ↓
将所有.html文件的hash.ttf引用都替换成压缩过的.woff2文件
      ↓
最后删除hash.ttf（可选）
```
具体的代码如下：<br>
由于此函数仅修改了`.html`文件，因此将函数名从`updateCssFontReferences`更改为`updateHtmlFontReferences`，文件名从`css-rewriter`更改为`html-rewriter`，要注意`index.js`中的`import`也应同步更改。
```js
//html-rewriter.js
import fs from "node:fs";
import path from "node:path";
import { ROOT_DIR, readFilesRecursively } from "./utils.js";
import { getFontConfigs } from "./config-parser.js";
export async function updateHtmlFontReferences() {
	console.log("[html-rewriter] started");
	try {
		const fonts = getFontConfigs();

		const distDir = path.join(ROOT_DIR, "dist/");
		const Files = readFilesRecursively(distDir).filter((f) =>
			f.endsWith(".html"),
		);
		if (Files.length === 0) {
			console.warn("⚠ No HTML files found in dist");
			return;
		}

		const deleteList = [];
		// 遍历所有字体
		for (const fontConfig of fonts) {
			for (const file of fontConfig.files) {
				const name=fontConfig.name;
				const woff2File = file.woff2Name;
				let hashName = null;
				const indexFile = path.join(ROOT_DIR, "dist/404.html");
				const indexContent = fs.readFileSync(indexFile, "utf8");
				
				//检测woff2文件是否被成功生成
				const distWoff2 = path.join(
					ROOT_DIR,
					`dist/_astro/fonts/${woff2File}`,
				);
				const hasWoff2 = fs.existsSync(distWoff2);
				if (!hasWoff2) {
					console.log(
						`⚠ No woff2 found for ${baseName}, keeping ttf reference`,
					);
					continue;
				}

				//获取 哈希值->字体 的映射向量
				const fontFacePattern = /@font-face\s*\{[^}]*\}/gs;
				let match;
				while ((match = fontFacePattern.exec(indexContent)) !== null) {
					const block = match[0];
					// fallback 字体没有 src:url
					if (!block.includes("src:url")) {
						continue;
					}
					// 提取 family
					const familyMatch = block.match(
						/font-family\s*:\s*["']?([^;"'}]+)["']?/,
					);
					if (!familyMatch) {
						continue;
					}
					const familyBase = familyMatch[1].replace(/-[0-9a-f]{16}$/, "");
					if (familyBase !== name) {
						continue;
					}
					// 提取 hash.ttf
					const srcMatch = block.match(
						/src:url\(["']\/_astro\/fonts\/([^"']+\.ttf)["']\)/,
					);
					if (!srcMatch) {
						continue;
					}
					hashName = srcMatch[1];
					break;
				}
				if (!hashName) {
					console.warn(`⚠ Cannot find hash font for ${name}`);
					continue;
				}else{
					deleteList.push(hashName);
				}
				console.log(`${name} -> ${hashName}`);//已经找到 哈希值->字体 的映射向量

				//遍历所有文件并替换引用
				for (const File of Files) {
					let Content = fs.readFileSync(File, "utf8");
					const originalContent = Content;

					Content = Content.replaceAll(
						hashName,
						woff2File,
					);
				
					Content = Content.replace(
        				new RegExp(
         				   `url\\((["'])?/_astro/fonts/${woff2File.replace(".", "\\.")}\\1\\)\\s*format\\((["'])truetype\\2\\)`,
        				    "g"
       					),
       					`url("/_astro/fonts/${woff2File}") format("woff2")`
    				);
				
					Content = Content.replace(
        				new RegExp(
         				   `href=(["'])/_astro/fonts/${woff2File.replace(".", "\\.")}\\1([^>]*?)type=(["'])font/ttf\\3`,
        				    "g"
        				),
        				`href="/_astro/fonts/${woff2File}"$2type="font/woff2"`
    				);
					if (Content !== originalContent) {
						fs.writeFileSync(File, Content);
							console.log(
							`✓ Updated File: ${File} (${woff2File})`,
						);
					}
				}
			}
		}
		//删除原来的ttf字体文件
		const astroFontDir = path.join(ROOT_DIR, "dist/_astro/fonts");
		for (const hashName of deleteList) {
   			const file = path.join(astroFontDir, hashName);
   			if (fs.existsSync(file)) {
   		    	fs.unlinkSync(file);
        		console.log(`🗑 Deleted ${hashName}`);
    		}
		}
	} catch (error) {
		console.error("⚠ HTML font reference update failed:", error.message);
	}
}
```
## 校验
运行build指令：
```bash
pnpm build
```
返回的`fonts`就是`astro.config.mjs`中的字体配置中的一部分，它包含了字体的用户名称，类型（不过均返回`cjkFont`）以及路径：
```bash
[
  {
    name: 'MarukoGothicCJKsc-Medium',
    type: 'cjkFont',
    files: [
      {
        src: './src/assets/fonts/MarukoGothicCJKsc-Medium.ttf',
        absolutePath: 'D:\\Workspace\\git\\NF3eb.github.io\\src\\assets\\fonts\\MarukoGothicCJKsc-Medium.ttf',
        fileName: 'MarukoGothicCJKsc-Medium.ttf',
        baseName: 'MarukoGothicCJKsc-Medium',
        ext: '.ttf',
        woff2Name: 'MarukoGothicCJKsc-Medium.woff2'
      }
    ]
  },
  {
    name: 'Loli',
    type: 'cjkFont',
    files: [
      {
        src: './src/assets/fonts/loli.ttf',
        absolutePath: 'D:\\Workspace\\git\\NF3eb.github.io\\src\\assets\\fonts\\loli.ttf',
        fileName: 'loli.ttf',
        baseName: 'loli',
        ext: '.ttf',
        woff2Name: 'loli.woff2'
      }
    ]
  }
]
```
成功压缩字体，并且大幅减小了字体文件的体积：
```bash
Starting font compression...
Compressing MarukoGothicCJKsc-Medium.ttf...
✓ MarukoGothicCJKsc-Medium.ttf → MarukoGothicCJKsc-Medium.woff2 (297.73 KB, reduced 98.52%)
Compressing loli.ttf...
✓ loli.ttf → loli.woff2 (217.95 KB, reduced 97.93%)
```
`.html`文件中的多种字体引用已经成功被覆写：
```bash
MarukoGothicCJKsc-Medium -> bb40865ce153a39b.ttf
✓ Updated File: D:\Workspace\git\NF3eb.github.io\dist\2\index.html (MarukoGothicCJKsc-Medium.woff2)
✓ Updated File: D:\Workspace\git\NF3eb.github.io\dist\404.html (MarukoGothicCJKsc-Medium.woff2)
✓ Updated File: D:\Workspace\git\NF3eb.github.io\dist\about\index.html (MarukoGothicCJKsc-Medium.woff2)
✓ Updated File: D:\Workspace\git\NF3eb.github.io\dist\archive\index.html (MarukoGothicCJKsc-Medium.woff2)
...
Loli -> cbc95adf6a989764.ttf
✓ Updated File: D:\Workspace\git\NF3eb.github.io\dist\2\index.html (loli.woff2)
✓ Updated File: D:\Workspace\git\NF3eb.github.io\dist\404.html (loli.woff2)
✓ Updated File: D:\Workspace\git\NF3eb.github.io\dist\about\index.html (loli.woff2)
✓ Updated File: D:\Workspace\git\NF3eb.github.io\dist\archive\index.html (loli.woff2)
...
```
对生成的静态网页进行预览：
```bash
pnpm preview
```
然后打开F12开发者工具，在顶部栏选择**Network**（网络），并勾选**Disable cahe**（禁用缓存），`Ctrl+F5`刷新，在搜索框中键入`font`，可以看到载入的字体是压缩过的`.woff2`文件，而非原先的`.ttf`文件：
<img src="/images/posts/20260806/1.png" height="400" width="400">

---
# 后记
和ChatGPT的探讨过程：
<img src="/images/posts/20260806/2.png" height="400" width="300">
<small>为什么不用codex？因为我被手机号卡了QAQ（虽然现在已经处理好了）</small><br>
笑点解析：
- 我一眼就发现了问题
- 我觉得你的思路总体是可行的
- 我赞同你的思路
- 完全可以
- 我还想确认一件事
- 我有一个怀疑
- 我还有一个怀疑（概率很高）
- 我又有一个怀疑（目前概率最高）
- 我现在反而怀疑的是另一个地方
- 我还有一个发现（我认为这很关键）
- 我还建议再进一步优化
- 这次一定能成功

太痛苦了😭太痛苦了😭
